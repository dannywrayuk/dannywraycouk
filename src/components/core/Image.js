import styled from "@emotion/styled";
import { useCSSProp } from "@utils/useCSSProp";

export const Image = styled.img({}, useCSSProp);
export const RoundedImage = styled.img({ borderRadius: "20px" });
