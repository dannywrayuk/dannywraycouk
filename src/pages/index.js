import { Navigation } from "@components/Navigation";
import { Layout } from "@components/Layout";

const Home = () => {
  return (
    <Layout>
      <Navigation />
    </Layout>
  );
};

export default Home;
